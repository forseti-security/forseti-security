rules:
  - name: Allow only symmetric key algorithms.
    mode: blacklist
    resource:
      - type: organization
        resource_ids:
          - '*'
    key:
       - algorithms:
         - GOOGLE_SYMMETRIC_ENCRYPTION
         protection_level: SOFTWARE
         purpose: symmetric
