- name: Allow my company users to be in my company groups.
  group_email: my_customer
  mode: whitelist
  conditions:
    - member_email: '@henrychang.mygbiz.com'

- name: allow mypartner users to be in specific group
  group_email: watchword-admins@henrychang.mygbiz.com
  mode: whitelist
  conditions:
    - member_email: '@gmail.com'

# Specific group can whitelist additional users.

- name: allow mypartner users to be in specific group
  group_email: ufo@henrychang.mygbiz.com
  mode: whitelist
  conditions:
    - member_email: '@gmail.com'

# Only employees can be added to group.
# TODO
